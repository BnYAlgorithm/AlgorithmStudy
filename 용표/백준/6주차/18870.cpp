#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
	int N;
	cin >> N;

	vector<long long> arr;		// ??? ???? ?? ???
	vector<long long> arr_res;	// ???????? ?? ??? ???
	for (int i = 0; i < N; i++)
	{
		long long tmp;
		cin >> tmp;
		arr.push_back(tmp);
		arr_res.push_back(tmp);
	}

	sort(arr_res.begin(), arr_res.end());
	arr_res.erase(unique(arr_res.begin(), arr_res.end()), arr_res.end());
	for (int num : arr)
		cout << lower_bound(arr_res.begin(), arr_res.end(), num) - arr_res.begin() << " ";

	// find ????? ????? ????? ????? ??????? ???? ?? ??? ?????? ???? ?????? 
	// erase?? unique?? ?????? ??? ????, ??????? ???? ?? ??? ?????? lower_bound?? ???
}